server:
    port: 8081

spring:
    cloud:
        config:
            server:
                git:
                    default-label: master
                    uri: https://github.com/khebinSd/java-techie-task
                    searchPaths: config-data
    security:
        user:
            name: root
            password: s3cr3t
